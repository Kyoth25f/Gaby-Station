- type: gameMap
  id: NewCentComm
  isGrid: true # Did you know that centcomm is the only "game map" that isn't actually a map? Send help.
  mapName: 'Central Command'
  mapPath: /Maps/_Gabystation/Centcomms/newComm.yml
  minPlayers: 10
  stations:
    centcomm:
      stationProto: NanotrasenCentralCommand
      components:
        - type: StationNameSetup
          mapNameTemplate: '{0} Central Command NT-14 {1}'
          nameGenerator:
            !type:NanotrasenNameGenerator
            prefixCreator: 'TG'
